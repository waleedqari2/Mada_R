# Mada_R - نظام إدارة طلبات الصرف

نظام متكامل لإدارة طلبات الصرف لشركة مدى السياحية، يشمل قاعدة بيانات MySQL، واجهة برمجية REST API، وواجهة مستخدم حديثة.

## المميزات الرئيسية

### 1. إدارة طلبات الصرف
- إنشاء طلبات صرف جديدة مع معلومات كاملة
- تعديل الطلبات الموجودة
- حذف الطلبات
- تتبع حالة كل طلب (تم التعميد، في انتظار التعميد، موافق عليها، تم التنفيذ)

### 2. تحويل المبالغ تلقائياً
- تحويل المبالغ من الأرقام إلى نص عربي تلقائياً
- دعم المبالغ حتى الملايين
- دعم الأجزاء العشرية (هللة)

### 3. واجهة مستخدم حديثة
- تصميم عصري بالعربية
- جداول تفاعلية لعرض البيانات
- نماذج سهلة الاستخدام
- إحصائيات سريعة

### 4. نظام الأقسام
- الإدارة
- الحسابات
- المبيعات
- التسويق
- الحجوزات
- العمليات

### 5. أنواع التصرف
- نقدي
- شيك
- تحويل

## البنية التقنية

### Backend (Node.js/Express)
- Express.js للخادم
- MySQL2 للاتصال بقاعدة البيانات
- TypeScript للسلامة النوعية
- RESTful API

### Frontend (React)
- React 19 مع TypeScript
- Wouter للتوجيه
- Radix UI للمكونات
- Tailwind CSS للتصميم
- Axios لطلبات API

### Database (MySQL 8.0)
- قاعدة بيانات MySQL
- دعم كامل للنصوص العربية (UTF-8)
- فهرسة محسنة للاستعلامات السريعة

## التثبيت والإعداد

### المتطلبات الأساسية
- Node.js 20 أو أحدث
- Docker و Docker Compose (للتطوير المحلي)
- MySQL 8.0 (في حالة عدم استخدام Docker)

### الخطوات

#### 1. استنساخ المشروع
```bash
git clone https://github.com/waleedqari2/Mada_R.git
cd Mada_R
```

#### 2. تثبيت التبعيات
```bash
npm install --legacy-peer-deps
```

#### 3. إعداد متغيرات البيئة
انسخ ملف `.env.example` إلى `.env` وقم بتعديل القيم:

```bash
cp .env.example .env
```

محتوى ملف `.env`:
```env
# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=mada_user
DB_PASSWORD=mada_password
DB_NAME=mada_requests

# Application Configuration
PORT=3000
NODE_ENV=development

# MySQL Root Password (for Docker setup)
MYSQL_ROOT_PASSWORD=root_password
```

#### 4. تشغيل قاعدة البيانات (باستخدام Docker)
```bash
docker-compose up -d
```

هذا الأمر سيقوم بـ:
- تشغيل MySQL 8.0
- إنشاء قاعدة البيانات تلقائياً
- تنفيذ ملف schema.sql لإنشاء الجداول

#### 5. تشغيل التطبيق في وضع التطوير
```bash
npm run dev
```

سيعمل التطبيق على `http://localhost:3000`

#### 6. بناء التطبيق للإنتاج
```bash
npm run build
npm start
```

## هيكل قاعدة البيانات

### جدول `requests`

| العمود | النوع | الوصف |
|--------|------|-------|
| `id` | INT (auto-increment) | المعرف الفريد للطلب |
| `requestNumber` | VARCHAR(50) | رقم الطلب المتسلسل (REQ-YYYY-XXXXX) |
| `paymentType` | ENUM | نوع التصرف (نقدي، شيك، تحويل) |
| `requesterName` | VARCHAR(255) | اسم مقدم الطلب |
| `jobTitle` | VARCHAR(255) | الوظيفة |
| `department` | ENUM | القسم |
| `approverDepartment` | ENUM | إدارة المعتمد |
| `amountInNumbers` | DECIMAL(12,2) | مبلغ الصرف بالأرقام |
| `amountInWords` | TEXT | مبلغ الصرف كتابتاً بالعربية |
| `status` | ENUM | حالة الطلب |
| `createdAt` | TIMESTAMP | تاريخ الإنشاء |
| `updatedAt` | TIMESTAMP | تاريخ آخر تعديل |

## واجهة برمجة التطبيقات (API)

### نقاط النهاية (Endpoints)

#### 1. إنشاء طلب جديد
```http
POST /api/requests
Content-Type: application/json

{
  "paymentType": "نقدي",
  "requesterName": "أحمد محمد",
  "jobTitle": "مدير مبيعات",
  "department": "المبيعات",
  "approverDepartment": "الإدارة",
  "amountInNumbers": 5000
}
```

**الاستجابة:**
```json
{
  "id": 1,
  "requestNumber": "REQ-2025-00001",
  "paymentType": "نقدي",
  "requesterName": "أحمد محمد",
  "jobTitle": "مدير مبيعات",
  "department": "المبيعات",
  "approverDepartment": "الإدارة",
  "amountInNumbers": 5000,
  "amountInWords": "خمسة آلاف ريال سعودي",
  "status": "في انتظار التعميد",
  "createdAt": "2025-12-24T12:00:00.000Z",
  "updatedAt": "2025-12-24T12:00:00.000Z"
}
```

#### 2. الحصول على جميع الطلبات
```http
GET /api/requests
```

#### 3. الحصول على طلب محدد
```http
GET /api/requests/:id
```

#### 4. تحديث طلب
```http
PUT /api/requests/:id
Content-Type: application/json

{
  "status": "تم التعميد",
  "amountInNumbers": 6000
}
```

#### 5. حذف طلب
```http
DELETE /api/requests/:id
```

## النشر على Railway

### الخطوات

1. تأكد من وجود ملف `Dockerfile` في المجلد الرئيسي
2. قم بإنشاء حساب على [Railway](https://railway.app)
3. أنشئ مشروع جديد واربطه بمستودع GitHub
4. أضف قاعدة بيانات MySQL من Marketplace
5. أضف متغيرات البيئة:
   - `DB_HOST`: عنوان قاعدة البيانات من Railway
   - `DB_PORT`: 3306
   - `DB_USER`: المستخدم من Railway
   - `DB_PASSWORD`: كلمة المرور من Railway
   - `DB_NAME`: اسم قاعدة البيانات
   - `NODE_ENV`: production
6. سيتم النشر تلقائياً عند كل push

### تنفيذ schema.sql على Railway
بعد إنشاء قاعدة البيانات، قم بتنفيذ الأمر SQL من ملف `server/database/schema.sql` من خلال:
- واجهة Railway MySQL Query
- أو أي أداة إدارة MySQL (مثل MySQL Workbench)

## الاستخدام

### صفحة عرض الطلبات (الصفحة الرئيسية)
- عرض البيانات الثابتة للطلبات
- تصدير PDF
- تحميل Excel
- الوصول إلى نظام الإدارة

### صفحة إدارة الطلبات
- إنشاء طلبات جديدة
- عرض جميع الطلبات في جدول تفاعلي
- تعديل الطلبات الموجودة
- حذف الطلبات
- تغيير حالة الطلبات
- عرض إحصائيات سريعة

## المساهمة

نرحب بالمساهمات! يرجى فتح Issue أو Pull Request.

## الترخيص

MIT License

## الدعم

للدعم والاستفسارات، يرجى التواصل مع فريق التطوير.

---

تم التطوير بواسطة فريق مدى السياحية 🚀