# 🚀 دليل النشر على Railway

هذا دليل شامل خطوة بخطوة لنشر نظام إدارة طلبات الصرف على Railway.

## 📋 المتطلبات الأولية

- حساب GitHub
- حساب Railway (مجاني) - [التسجيل هنا](https://railway.app)
- المشروع موجود على GitHub repository

## 🎯 الخطوة 1: إنشاء حساب Railway

1. اذهب إلى [Railway.app](https://railway.app)
2. انقر على "Start a New Project"
3. سجّل الدخول باستخدام GitHub

## 🗄️ الخطوة 2: إضافة قاعدة بيانات MySQL

1. في لوحة Railway، انقر على "New Project"
2. اختر "Provision MySQL"
3. انتظر حتى يتم إنشاء قاعدة البيانات

### الحصول على معلومات الاتصال

بعد إنشاء قاعدة البيانات:
1. انقر على خدمة MySQL
2. اذهب إلى تبويب "Variables"
3. ستجد المتغيرات التالية:
   - `MYSQLHOST`
   - `MYSQLPORT`
   - `MYSQLUSER`
   - `MYSQLPASSWORD`
   - `MYSQLDATABASE`

## 🌐 الخطوة 3: إضافة التطبيق

1. في نفس المشروع، انقر على "New Service"
2. اختر "GitHub Repo"
3. اختر repository المشروع (`waleedqari2/Mada_R`)
4. Railway سيكتشف تلقائياً وجود Dockerfile

## ⚙️ الخطوة 4: إعداد المتغيرات البيئية

1. انقر على خدمة التطبيق
2. اذهب إلى تبويب "Variables"
3. أضف المتغيرات التالية:

### المتغيرات الأساسية (مطلوبة)

```bash
# Database Configuration
DB_HOST=${{MySQL.MYSQLHOST}}
DB_PORT=${{MySQL.MYSQLPORT}}
DB_USER=${{MySQL.MYSQLUSER}}
DB_PASSWORD=${{MySQL.MYSQLPASSWORD}}
DB_NAME=${{MySQL.MYSQLDATABASE}}

# Server Configuration
NODE_ENV=production
PORT=3000

# JWT Configuration (استخدم مفتاح عشوائي قوي)
JWT_SECRET=your-very-secret-random-key-change-this-123456789
JWT_EXPIRY=7d

# CORS (اختياري - يمكن تركه * أو تحديد domain)
CORS_ORIGIN=*
```

### توليد JWT_SECRET آمن

استخدم أحد هذه الطرق لتوليد مفتاح عشوائي:

**طريقة 1: Node.js**
```bash
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
```

**طريقة 2: OpenSSL**
```bash
openssl rand -hex 64
```

**طريقة 3: Online Generator**
استخدم موقع مثل [randomkeygen.com](https://randomkeygen.com/)

### المتغيرات الاختيارية (للبريد الإلكتروني)

إذا كنت تريد إرسال إشعارات بالبريد الإلكتروني:

```bash
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@mada.sa
```

**ملاحظة:** لـ Gmail، ستحتاج إلى إنشاء App Password من إعدادات الحساب.

## 🔧 الخطوة 5: إعداد قاعدة البيانات

بعد نشر التطبيق لأول مرة، تحتاج لإعداد قاعدة البيانات:

### الطريقة 1: استخدام Railway CLI (موصى بها)

1. ثبت Railway CLI:
```bash
npm install -g @railway/cli
```

2. سجّل الدخول:
```bash
railway login
```

3. اربط المشروع:
```bash
railway link
```

4. شغّل سكريبت الإعداد:
```bash
railway run pnpm run db:setup
```

### الطريقة 2: استخدام MySQL Client مباشرة

1. احصل على بيانات الاتصال من Railway
2. اتصل بقاعدة البيانات باستخدام MySQL Workbench أو CLI:
```bash
mysql -h [MYSQLHOST] -P [MYSQLPORT] -u [MYSQLUSER] -p[MYSQLPASSWORD] [MYSQLDATABASE]
```

3. نفّذ محتوى ملف `server/database/schema.sql`

## 🌍 الخطوة 6: إعداد Domain (اختياري)

### استخدام Railway Domain

Railway يوفر لك domain مجاني تلقائياً بصيغة:
```
your-app-name.up.railway.app
```

### استخدام Custom Domain

1. في خدمة التطبيق، اذهب إلى "Settings"
2. انقر على "Generate Domain" أو "Custom Domain"
3. أضف domain الخاص بك
4. أضف CNAME record في DNS provider:
   - Name: `www` أو `@`
   - Value: `your-app-name.up.railway.app`

## 🔍 الخطوة 7: التحقق من النشر

1. انتظر حتى يكتمل النشر (عادة 2-5 دقائق)
2. افتح URL التطبيق
3. يجب أن ترى صفحة تسجيل الدخول

### اختبار الاتصال بقاعدة البيانات

زر endpoint الـ health check:
```
https://your-app.up.railway.app/api/health
```

يجب أن يعيد:
```json
{
  "status": "ok",
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

## 📊 الخطوة 8: المراقبة والصيانة

### عرض Logs

1. في Railway، انقر على خدمة التطبيق
2. اذهب إلى تبويب "Deployments"
3. انقر على آخر deployment
4. اعرض "Build Logs" و "Deploy Logs"

### مراقبة الموارد

1. اذهب إلى تبويب "Metrics"
2. راقب:
   - CPU Usage
   - Memory Usage
   - Network Traffic

### إعادة التشغيل

إذا احتجت لإعادة تشغيل التطبيق:
1. اذهب إلى "Settings"
2. انقر على "Restart"

## 🔄 تحديث التطبيق

### Auto-deployment من GitHub

Railway تلقائياً ينشر التحديثات عند:
- Push جديد على الـ branch المحدد
- Merge Pull Request

### Manual Deployment

1. في خدمة التطبيق، اذهب إلى "Deployments"
2. انقر على "Deploy"

## 🐛 حل المشاكل الشائعة

### المشكلة 1: التطبيق لا يعمل بعد النشر

**الحل:**
1. تحقق من Logs للأخطاء
2. تأكد من جميع المتغيرات البيئية صحيحة
3. تحقق من اتصال قاعدة البيانات

### المشكلة 2: خطأ في الاتصال بقاعدة البيانات

**الحل:**
1. تأكد من أن خدمة MySQL تعمل
2. تحقق من متغيرات `DB_*`
3. تأكد من استخدام `${{MySQL.VARIABLE}}` syntax

### المشكلة 3: Build يفشل

**الحل:**
1. تحقق من Build Logs
2. تأكد من أن `package.json` صحيح
3. تحقق من أن جميع dependencies موجودة

### المشكلة 4: الـ token expired

**الحل:**
1. تحقق من أن `JWT_SECRET` مضبوط
2. تأكد من أن `JWT_EXPIRY` منطقي (مثل `7d`)

## 💰 التكلفة والحدود

### Free Plan
- 500 ساعة تشغيل شهرياً
- $5 رصيد شهري مجاني
- مناسب للتطوير والتجريب

### Pricing
- Web Service: ~$5/شهرياً
- MySQL: ~$5/شهرياً
- إجمالي تقريبي: ~$10/شهرياً

**ملاحظة:** الأسعار قد تتغير، راجع [Railway Pricing](https://railway.app/pricing)

## 🔐 أفضل الممارسات الأمنية

1. ✅ **لا تشارك** `JWT_SECRET` أبداً
2. ✅ استخدم **مفتاح عشوائي قوي** لـ JWT
3. ✅ فعّل **Environment Variables** بدلاً من hardcoding
4. ✅ راجع **Logs** بانتظام
5. ✅ احتفظ بـ **نسخة احتياطية** من قاعدة البيانات

## 📧 النسخ الاحتياطي لقاعدة البيانات

### تصدير يدوي

```bash
railway run mysqldump -h $DB_HOST -P $DB_PORT -u $DB_USER -p$DB_PASSWORD $DB_NAME > backup.sql
```

### استيراد من نسخة احتياطية

```bash
railway run mysql -h $DB_HOST -P $DB_PORT -u $DB_USER -p$DB_PASSWORD $DB_NAME < backup.sql
```

## 🎓 موارد إضافية

- [Railway Documentation](https://docs.railway.app)
- [Railway Discord Community](https://discord.gg/railway)
- [Railway Blog](https://blog.railway.app)

## ✅ Checklist النشر

- [ ] إنشاء حساب Railway
- [ ] إضافة MySQL Database
- [ ] إضافة التطبيق من GitHub
- [ ] إضافة جميع المتغيرات البيئية
- [ ] توليد `JWT_SECRET` قوي
- [ ] إعداد قاعدة البيانات (db:setup)
- [ ] اختبار صفحة تسجيل الدخول
- [ ] اختبار API endpoints
- [ ] مراجعة Logs
- [ ] إعداد Custom Domain (اختياري)
- [ ] إعداد نسخ احتياطي

---

## 🆘 الحصول على المساعدة

إذا واجهت أي مشاكل:
1. راجع هذا الدليل مرة أخرى
2. تحقق من Logs في Railway
3. افتح Issue على GitHub
4. انضم إلى Railway Discord

**نتمنى لك نشراً موفقاً! 🚀**
