# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=password
DB_NAME=mada_requests

# Server Configuration
PORT=3000
NODE_ENV=production
API_URL=http://localhost:3000

# JWT Configuration
JWT_SECRET=your-secret-key-change-this-in-production-use-strong-random-string
JWT_EXPIRY=7d

# CORS Configuration
CORS_ORIGIN=*

# Email Configuration (NodeMailer) - Optional
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@mada.sa

# Railway Specific Variables (Set automatically by Railway)
# RAILWAY_ENVIRONMENT_NAME=production
# RAILWAY_SERVICE_NAME=mada-requests
# DATABASE_URL=mysql://user:pass@host:port/database
